- name: Homelab
  columns:
    - size: full
      widgets:
        - type: releases
          token: ${GITHUB_TOKEN}
          cache: 1d
          repositories:
            - glanceapp/glance
            - curl/curl
